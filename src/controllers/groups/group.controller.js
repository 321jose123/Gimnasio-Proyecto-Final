const { newGroup } = require('./newGroup');
const { searchGroup } = require('./searchGroup');
const { listAllGroups } = require('./listAllGroups');

module.exports = {
    newGroup,
    searchGroup,
    listAllGroups,
}